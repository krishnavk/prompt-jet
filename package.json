{
  "name": "prompt-jet",
  "version": "1.0.0",
  "description": "Full-stack LLM prompt execution platform",
  "private": true,
  "workspaces": [
    "apps/console",
    "apps/server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:console\" \"npm run dev:server\"",
    "dev:console": "cd apps/console && npm run dev",
    "dev:server": "cd apps/server && npm run start:dev",
    "build": "npm run build:console && npm run build:server",
    "build:console": "cd apps/console && npm run build",
    "build:server": "cd apps/server && npm run build",
    "audit": "npm audit",
    "audit:fix": "npm audit fix",
    "security:check": "npm run audit && cd apps/console && npm audit && cd ../server && npm audit"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
